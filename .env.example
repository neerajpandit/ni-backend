# Server
NODE_ENV=development
PORT=5000

# MongoDB — URI without trailing slash; database name below is applied separately
MONGODB_URI=mongodb+srv://anchalpandeywork:anchal1234@cluster0.mrzza.mongodb.net
DB_NAME=campussphere

# JWT (use a long random string in production)
JWT_SECRET=change-this-to-a-long-random-secret-in-production
JWT_EXPIRES_IN=7d

# CORS — comma-separated origins. In NODE_ENV!=production, http://localhost:* and http://127.0.0.1:* are also allowed for Vite.
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
